settings:
  legacy:
    force: false
    interval: 100ms
schema:
- name: webapp
  path: .
  commands:
    run:
        status: true
  watcher:
    extensions:
    - go
    - html
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
